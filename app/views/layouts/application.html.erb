<!DOCTYPE html>
<html>
  <head>
    <title>ChatOnlineApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <div class="container">
      <div class="d-flex justify-content-between">
        <a class="navbar-brand" href="#">ChatOnlineApp</a>
        <div>
          <% if current_user.present? %>
            <span class="navbar-text mr-3">Signed in as: <%= current_user.email %></span>
            <%= link_to "Sign Out", destroy_user_session_path, data: { "turbo-method": "delete" }, class: "btn btn-secondary" %>
          <% else %>
            <%= link_to "Sign In", new_user_session_path, class: "btn btn-primary mr-2" %>
            <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary" %>
          <% end %>
        </div>
      </div>
    </div>
    <%= yield %>
  </body>
</html>
